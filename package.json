{
  "name": "nexus-ecosystem",
  "version": "0.1.0",
  "private": true,
  "description": "NEXUS AI — AI-powered Web3 operating system for founders",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "npx playwright test --project=chromium",
    "test:headed": "npx playwright test --project=chromium --headed",
    "test:mobile": "npx playwright test --project=mobile-chrome",
    "test:cross-browser": "npx playwright test",
    "test:demo": "npx playwright test --config=playwright.demo.config.ts",
    "test:report": "npx playwright show-report playwright-report/html",
    "test:full": "npx playwright test && npx playwright test --config=playwright.demo.config.ts",
    "postinstall": "npx patch-package"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "patch-package": "^8.0.1",
    "playwright": "^1.61.1",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.9.2",
  "optionalDependencies": {
    "@tailwindcss/oxide-linux-x64-gnu": "4.3.1",
    "@tailwindcss/oxide-linux-x64-musl": "4.3.1"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2"
  }
}
